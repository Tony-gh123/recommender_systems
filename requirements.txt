numpy<2.0
pandas
scipy
scikit-learn
matplotlib
tqdm
joblib
pyarrow
implicit
kagglehub
notebook
ipykernel

# Remove if running pip install -r requirements.txt
# Create venv
py -3.11 -m venv .venv

# Upgrade core tools
.\.venv\Scripts\python.exe -m pip install --upgrade pip setuptools wheel

# Install packages (Note: numpy<2.0 is crucial for current ecosystem compatibility)
.\.venv\Scripts\python.exe -m pip install "numpy<2.0" pandas scipy scikit-learn matplotlib tqdm joblib pyarrow notebook ipykernel kagglehub implicit

# Register kernel
.\.venv\Scripts\python.exe -m ipykernel install --user --name=recsys-py311 --display-name "Python 3.11 (RecSys)"
